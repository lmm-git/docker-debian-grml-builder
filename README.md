# Image to build grml with latest Debian
This is the source file for a docker image to build grml against the latest Debian.

Docker Hub page: https://hub.docker.com/r/lmmdock/docker-debian-grml-builder/
